server {
  listen   80;
  server_name  <%= node[:nginx_config][:server_name] %>;

  access_log  /var/log/nginx/<%= node[:nginx_config][:server_name] %>.access.log;

  location / {
    root   /var/www/nginx-default;
    index  index.html index.htm;
  }
}
